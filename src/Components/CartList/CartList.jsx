import React from 'react';
import PropTypes from 'prop-types';

const CartList = props => {
    return (
        <div className='w-1/4'>
            cartList
        </div>
    );
};

CartList.propTypes = {
    
};

export default CartList;